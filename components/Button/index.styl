.root
  justify-content center
  height 44px
  width 100%
  margin-top 2u
  border-width 1px
  border-color $textColor
  border-radius 1u
  &.blue
    background-color: $accentColor
    border-color $accentColor

  &.transparent
    border-color $white

.buttonText
  color $textColor
  text-align center
  &.blue,
  &.transparent
    color $white
